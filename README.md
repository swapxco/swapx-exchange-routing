# Swapx Routing Smart Contracts
Swapx router smart contracts, interfaces, libraries and deployment scripts

## NPM scripts

 * `npm run test`
 * `npm run build`
 * `npm run compile`
 * `npm run serve`
 * `npm run flatten`
 * `npm run size`